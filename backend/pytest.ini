[pytest]
DJANGO_SETTINGS_MODULE = prepcheck.settings_test
python_files = tests.py test_*.py *_tests.py
# Build the schema directly from models (fast, and lets the test-only
# tests_support app work without a checked-in migration). Real apps still ship
# their own migrations for production.
addopts = --no-migrations -q
